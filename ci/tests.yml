---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: '1.7'

inputs:
- name: simple-go-webapp
  path: gopath/src/github.com/tryggth/concourse-example

run:
  path: bash
  args: ['-c', 'go get; go build -o app; go test; ls -la']
  dir: gopath/src/github.com/tryggth/concourse-example
